let lapCompleted = 0;

//Criar uma função que aumenta a variável "lapCompleted" em um e executar a função três vezes

function incremento(){
    lapCompleted = lapCompleted + 1;
}

incremento();
console.log(lapCompleted);
incremento();
console.log(lapCompleted);
incremento();
console.log(lapCompleted);